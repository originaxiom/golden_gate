# THE SPENDING ORDER — the hatch DAG, the cosmological match, and what the 27 is in the gravity reading
## (outside bench, 2026-08-21; fourth memo; computation `spending_order_out.txt`)

**The question made precise.** "Universe as big hatch" has testable content iff the free-bit
spendings have a FORCED partial order — X ≺ Y meaning: the choice Y is not even DEFINABLE
until X is spent. Definitional dependency, not dynamics. That order can then be compared to
the cosmological freezing sequence, which is the only thing the Big Bang's interior view
measures. Every edge below is a theorem or a banked fact; the two new computations are named.

## 1. THE DAG (each edge with its warrant)

- **D0 — the rule** (C and P spent; the basepoint taken): nothing else is definable before
  the substitution exists. [B1083]
- **D1 — the double tick** (orientability purchased): the spin QUESTION is not posable
  before — a spin structure requires an orientation (theorem); the one-tick object (the
  Gieseking) has none. [B1083/V.5]
- **D2 — the geometric holonomy**: FORCED, zero bits (Mostow rigidity). Two things become
  available here: gravity-as-geometry (the object IS a Λ=−1 Einstein solution, B259), and
  the signature-as-READING — PSL(2,ℂ) = SO⁺(3,1) is already the Lorentz group. But
  signature-as-STRUCTURE is not: the centralizer of the irreducible holonomy is trivial
  (tr[A,B] ≠ 2, banked ⟹ Schur), so **no Lorentz-partner sl₂ exists before an internal
  space is chosen** — the realification pair requires the stratum.
- **D4 — the stratum** (the hatch; the projective menu; A2 the unique projective
  SM-compatible landing): the rank-4 Cartan, the 18 hypercharge directions (B1102 "at this
  stratum"), the EW seat (B1098), and the Lorentz pair (third memo) are all definable ONLY
  after this node.
- **D5 — THE FORK (double-duty, now order-typed)**: the non-color su(3) factor must serve
  either the EW seat or the Lorentz partner — exclusive by the joint-centralizer
  computation (third memo). This is a genuine branch point of the DAG, not an ordering.
- **D6 — the hypercharge direction** (18, ≈4.17 bits pending the orbit lead): after D4.
- **D7 — the VEV** (condition forced = the closed orbit; point free): after D6.
- **External leaves, attachable only at cuts/edges**: the spin bit (the lift the object
  refuses — projective hatch), the chirality closing (B994), the realification datum (the
  observer's conjugation — third memo), the scale (the substrate).

**Forced linearization:** rule → tick² → holonomy (gravity + signature-reading) → GUT
landing → {EW ⊻ internal-Lorentz} → hypercharge → VEV/values; fermions/chirality/scale at
the edges throughout.

## 2. THE COSMOLOGICAL MATCH (zero inversions)

Interior view (standard cosmology, early → late): initial data fixed → classical spacetime
with Lorentz signature → GUT-scale breaking → electroweak breaking → mass/value freezing;
fermionic content and chirality present as inputs throughout. Against the DAG: **every
definitional dependency is consistent with the temporal freezing order — no edge is
inverted.** (Precedent: B1082 verified the cascade's forcing DAG against the cosmological
order, conditional-on-GUT-model; this extends that check across the full spending ledger.)
The honest two-level note: cosmological spacetime is Lorentzian from the start — matched by
the signature-READING at D2 (observer-side, pre-GUT); the internal/unified Lorentz option
only exists at D5 and was NOT taken (next paragraph). The falsifier is stated: any
cosmologically forced ordering that inverts a DAG edge kills the hatch reading.

## 3. THE FORK'S PUNCHLINE — why gravity is not a gauge force

The observed universe has an electroweak sector, i.e. nature's spending at D5 was EW. The
internal-Lorentz option — gravity unified inside E₆ — was thereby forgone: **gravity
remains external/geometric BECAUSE the slot it would occupy in E₆ is occupied by the
electroweak sector.** The oldest asymmetry of physics (three gauge forces + one geometric
one) becomes, in this frame, a record of a single binary spending. Interpretive register,
but the exclusivity itself is computed, not conjectured.

## 4. NEW VERIFIED FACT — the 27 in the gravity reading (the crown of the computation)

The joint (h₁,h₂) bi-weight spectrum of the 27 under the two commuting A2 triples, exact:
**{(±2,±2):1, (±2,0):4, (0,±2):4, (0,0):7} — matching the trinification prediction
cell-for-cell, and ALL bi-weights even.** Consequences:

- Under the constructed so(3,1), the 27 decomposes as **(1,1)⊗1_c ⊕ (1,0)⊗3_c ⊕ (0,1)⊗3̄_c**:
  a 9-state (1,1) color-singlet — **the traceless-symmetric rank-2 slot: the metric/spin-2
  representation** — plus self-dual and anti-self-dual colored vectors. Representation-level
  statement only (the right Lorentz types occupy the right slots); no graviton dynamics
  claimed.
- **The same 27 that is matter in the gauge reading is geometry (metric + colored flux) in
  the gravity reading.** The double-duty theorem extends from the algebra to the content:
  matter/metric are the two readings of one multiplet, selected by the D5 fork.
- **No half-integer spin exists anywhere at the landing.** Fermionicity requires the spin
  bit — the one purchase the object refuses (projective hatch). This extends banked B428
  (E₆ level 1 anyonic/bosonic-only, the 27 = [17,9,1]) to the Lorentz level, and unifies
  three externals into one: **the spin bit = fermionicity = chirality's supplier (B994's
  registerability) — one purchase, three faces, always paid by the observer.**

## 5. WHAT "THE UNIVERSE IS A BIG HATCH" NOW MEANS, EXACTLY

The Big Bang is the interior (temporal) view of the register's left edge; the freezing
sequence is the shadow of the spending order; the initial-conditions problem is the priced
torsor ledger (choice exists, chooser doesn't, ~a dozen bits + one scale, unrepeatable by
B1104); and the universe's contingent content — hand, vacuum, EW-not-unified-gravity,
fermions — is the record of which points were taken at the edge. Banked support: B1082
(order match), B1104 (no repeat), the edge results (free bits live at boundaries), this
memo's DAG and bi-weight table. Not claimed: heat, expansion, any dimensionful content —
the firewall stands.

**Proposed cells.** C-S1: promote the DAG to an arc (each edge one lock; the fork one
computation — all already exist). C-S2: the (1,1) slot's status — does the 3d action's
boundary graviton (B1107's Ruelle tower) couple to the landing's (1,1) block? A typed
bridge question, two-outcome. C-S3: the B428 join — level-1 bosonic-only and the all-even
bi-weight table as one statement across registers.
