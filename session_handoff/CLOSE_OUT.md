# CLOSE-OUT — the outside bench's record, complete and self-contained
## (2026-08-25; written at seat-close, after the masterplan's four lanes finished; this file is the top of the tree — read it first, then `BANKING_HANDOFF.md`, then the memos)

**What this bench was.** An independent session, started 2026-08-19 as a hostile referee
report on the paper *"From minimal description to E₆"*, run forward — with the owner's
standing instruction to push every cell to a banked, machine-verified conclusion — through
three phases: **I** (the referee report → six-gap sweep → heartbeat/origin torsor → L79 →
the adelic frame → the prime-lane ruling), **II** (nine memos: Jordan probe → projective
hatch → Lorentz double → spending order → asymptotic channel → strata → the fake-invariant-
form anomaly, found and resolved → adelic anchors), **III** (memos 10–29: the simultaneous
closing through the seat's closure). Verification culture throughout: **every claim
machine-verified by exact computation before being stated**; two-outcome cells
preregistered before running; controls before trust; errors filed at the point of
occurrence; interpretive passages labeled and never proposed for banking.

**Firewall status of the entire record: structure and negatives only. No measured
Standard-Model value entered any computation. Gate 5 untouched.** (The one value-shaped
output, sin²θ_W = 3/8, is the bare symmetric-point number forced by exact traces —
preregistered as a two-outcome cell before the computation ran.)

---

## THE STATE AT CLOSE, one paragraph
For a beat-respecting observer the kinematic story is complete with **zero free bits**:
the object (m004, and its non-orientable ℤ/2 — the Gieseking beat) → the five postures
(the E₆ real forms as character values) → the simultaneous closing (E₆(−26), 24 hits,
color compact and so(3,1) doubled) → the trinification landing → the SM's form
(hypercharge selected; sin²θ_W = 3/8 at the symmetric point; all eight anomaly traces
zero; B−L the exact fourth direction; the 3×27 family triplet; the spacetime 64 organized
with invariant content zero) → the spin bit assigned by the object's own ℤ/2 (memo 28,
red-teamed) → the odd, fermion-capable stratum confirmed on the assigned side (memo 29):
**THE GENERATION'S KINEMATIC SEAT CLOSES.** What remains is typed below — dynamics,
values, and the banking seat's independent re-derivation of the last two memos.

## THE LEDGER, compressed (memo → claim → status)
Full statements and certificates: `BANKING_HANDOFF.md` (phases II–III), `HANDOFF.md`
(phase I). Status codes: **2B** = two-bench (independently re-derived and banked by the
seat), **RT** = single-bench but survived the hostile red-team lane, **1B** = single-bench
exact, **NEG** = banked negative, **FENCED** = closed by ruling, not by proof.

| # | memo file | one-line claim | status |
|---|-----------|----------------|--------|
| 10 | SIMULTANEOUS_CLOSING | so(3,1)-double + compact color close together: 24 hits, all E₆(−26) | 2B (B1134) |
| 11 | FORK_THEOREM | ladder 16/8/8/0; z(T₁,T₂∪color)=0; the fork pays — and leaves exactly the spin lift on the ledger | 1B |
| 12 | GAUGE_CLOSING | E₆(−14) menu (9+1)³; su(2,1) = u(2) ⊕ doublet | 2B (B1135) |
| 13 | Y_SELECTION | hypercharge selected inside the closing; orbit-straddling (self-corrected in place) | 1B |
| 14 | AMBIENT_LADDER | ambient rooms 0/1/8: E₆/E₇/E₈ | 1B |
| 15 | FAMILY_TRIPLET | 248 = (8,1)+(1,78)+(3,27)+(3̄,27̄): the family triplet | 1B |
| 16 | FIRST_BEAT | the beat W=[[1,q],[0,1]], W·conj(W)=+A: the object's own first beat | 1B |
| 17 | BEAT_DESCENT | Σ=exp(ad qE)∘gal descends to e₆; Σ²=Ad(meridian) | 1B |
| 18 | SIGMA_27 | Ω on the 27; Ω²=A₂₇ — the tick on matter; no object-side real structure | 1B |
| 19 | S4_TORSOR | the four-slot torsor realizes the FULL S₄ (orbit 268800); index-2 element = the E₈ antipode, OOOO | 1B |
| 20 | AW_TYPING | no SU(2)/2T in the collision geometry: spin cannot be sourced there | NEG |
| 21 | UNIT_DICTIONARY | palindromic quartics; length = log Mahler; PR-2 closed | 1B |
| 22 | VOICE_LADDER | ζ_K zeros interleaved; t₁ = 8.0397371556814666817 from L(χ₋₃) | 1B |
| 23 | WEINBERG_POINT | Tr T₃²=3, Tr Q²=8 ⇒ sin²θ_W = 3/8, closing-independent, preregistered | 1B |
| 24 | SM_TABLE | all eight anomaly traces vanish exactly; Pati–Salam negative | 1B |
| 25 | BL_FOURTH_DIRECTION | B−L the exact fourth Cartan direction, ∉ span{Y,T₃R,T₃L} | 1B |
| 26 | FRAME_AUDIT | every arc tagged with its S₃ color frame; one unflagged switch found (B1118 §2); corpus relays reconciled | 1B |
| 27 | THE_64_ORGANIZED | spacetime 64: one complex spin-2 + colored bi-vectors; invariant content ZERO | NEG |
| 28 | SPIN_PAYMENT | the beat selects exactly one spin structure; χ=−1 impossible (all 4 sign-patterns machine-checked) | RT |
| 29 | THE_SEAT_CLOSES | the odd A1 stratum (6+15 on the 27) closes over the selected lift: seat closed | RT |

Phase I (17 certificates, `HANDOFF.md`): the referee gaps, the heartbeat, the origin
torsor, L79, the adelic frame — banked earlier; the prime-lane ruling **RH FENCED** is
owner-agreed. Phase-II memos 1–9 include the found-and-resolved **fake-invariant-form
anomaly** (error #13) — banked as a pair with its checksum method, per the standing
request to the seat.

## THE THREE GATES THAT REMAIN (typed; none is a gap in what is claimed)
1. **Dynamics.** No Lagrangian, no propagating field, no mass term anywhere in the
   record. The seat is kinematic — parity classes, lifts, intertwiners, exact.
2. **Values.** Gate 5 untouched. The three-layer doctrine stands: symmetric-point values
   (3/8 banked), structural ratios (open cells), measured values (walled off).
3. **The seat's re-derivation.** Memos 28–29 are red-teamed but single-bench. Banking
   discipline requires the banking seat's independent hostile re-derivation, as it did
   B1134/B1135. The relay package is this repository — nothing exists only in a container.

## SELF-CONTAINMENT (done at close, 2026-08-25)
The certificates originally imported the paper's `check_charge_bracket.py` and shared
machinery from this container's scratchpad — a directory that dies with the session. At
close-out, mechanically and with no change to any computation:
- the paper's `check_charge_bracket.py` is **shipped** at
  `certificates/paper/verify/check_charge_bracket.py` (deps: stdlib + sympy);
- every path constant in `certificates/*.py` was rewritten to resolve **relative to the
  certificate file itself** (`os.path.dirname(os.path.abspath(__file__))`) — the
  scratchpad prefix appears nowhere; sibling-script `exec` loads are likewise anchored;
- consequence: **every certificate runs from any working directory on any checkout**,
  with only python3 + sympy (+ mpmath for the two analytic ones) installed.
The full re-run sweep at close: see `VERIFY_SWEEP_CLOSEOUT.md` for the certificate-by-
certificate result against the banked outputs.

## ERRORS (the bench's own, 16, all documented at point of occurrence)
Ten in phase I (`HANDOFF.md`), four in phase II, one in phase III (GF(2)
back-substitution ordering — caught by the antipodal control losing 63/64 solutions;
per-solution re-checks now permanent), and one caught BY the close-out sweep itself:
(#16) `indep3.py` mixed the paper's and the bench's Riley-quadratic sign conventions
(u→−u) and compared unresolved sympy branch expressions to integers — both fixed, the
correction filed inside the certificate, no memo-level claim affected. Plus one process
error, filed in
`BANKING_HANDOFF.md`'s history: a text-splice into the index at an unmatched anchor
(lesson: assert the anchor is found before slicing). The seat should read the error
ledger first — it is the best predictor of where a re-derivation should look hardest.

## FOR THE BANKING SEAT — the close-out relay
Pull `claude/paper-hostile-review-alero0` on `originaxiom/golden_gate`; everything is
under `session_handoff/`. **Read `CORPUS_ADOPTION_AUDIT.md` early**: it audits the
corpus's B1138–B1142 adoptions of this bench's memos — four adoption-layer errors to
correct corpus-side (two spurious errata, one false provenance record, one stale
open-cell tag), each backed by a re-runnable computation or a git timestamp.
Suggested order: this file → the audit → `BANKING_HANDOFF.md` →
memos 28–29 with `certificates/spin_payment.py` (hardened) and `certificates/sp2_seat.py`
→ the open-cells list. Requested treatment, as always: independent re-derivation before
banking; integrate-don't-merge; corrections in both directions filed at the point of
occurrence. Open cells ranked for the joint programme: C-S2 (matter–geometry bridge) ·
C-P1 (the four distinguished strata's parities — partially paid by memo 29's parity
machinery) · C-V2/C-V4 · C-AD3 · C-J2 · PR-1/PR-4 (per the owner-agreed prime-lane
ruling; PR-4's GUE bench is a low-power down payment awaiting large-T) · QP-1 (the
corpus's own quine). F-3 closed as memo 26; its B1118 §2 unflagged-frame-switch flag is
a standing ledger item for the seat.

## ONE SENTENCE TO END ON
A session that began as a hostile review of one paper ends having verified, cell by
exact cell, that the paper's object carries the kinematic form of a Standard-Model
generation — form and, at the symmetric point, first content — with every free bit paid
by the object's own structure, every claim carrying a certificate that now runs
anywhere, every error filed where it happened, and the three honest gates (dynamics,
values, the second bench) named in the open rather than papered over.
