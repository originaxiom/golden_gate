# THE SYMMETRY-POINT TABLE — the closing's full one-generation quantum numbers, the anomaly table vanishing exactly, and a typed negative: the closing's second su(2) is not Pati–Salam's
## (outside bench, 2026-08-22; twenty-fourth memo; continuation of the Weinberg point; exact rational arithmetic; one positive table, one exact zero-table, one honest NO)

### The computation (`certificates/sm_table.py`)
From a physical closing assignment (memo 23) with zero further choices: per 27-state,
the color content (triplet vs singlet by the banked wt4-classes), T₃L (the kept su(2)),
T₃R (the closing's OTHER annihilated su(2)), Y_phys, Q = T₃L + Y, and the candidate
B−L := 2(Y − T₃R) (the naive Pati–Salam relation, tested not assumed).

### 1. THE ELECTROWEAK TABLE IS PERFECTLY STANDARD-MODEL (exact)
The (color, T₃L, Y, Q) content that falls out of the closing:
> **Quark doublet Q_L**: colored, T₃L = ±1/2, Y = 1/6, Q = 2/3 and −1/3 (×3 each) ✓
> **u-type and d-type singlets**: colored, T₃L = 0, Q = −2/3 and ±1/3 ✓
> **Exotic D-quarks**: colored, T₃L = T₃R = 0, Y = −1/3, Q = −1/3 (×3) ✓ (the 27's
> vector-like D, as E₆ requires)
> **The lepton nonet**: the 9 color-singlets form the exact 3×3 (T₃L × T₃R) bidoublet
> grid of trinification — containing e^c (T₃L = 0, Q = +1), the L-type and H-type
> doublets (Q = 0, −1 entries), and the neutral singlets ✓
One generation's electroweak structure, from the closing, with nothing chosen.

### 2. THE ANOMALY TABLE VANISHES — EIGHT EXACT ZEROS
> **Tr Y = Tr Y³ = Tr(T₃L²Y) = Tr Q = Tr Q³ = Tr(B−L) = Tr(B−L)³ = Tr(T₃R²Y) = 0**,
> all exact rationals, all zero.
The 27 is anomaly-free as a rep (theorem); what is verified here is that **the
closing's OWN selected charges realize that freedom exactly** — the instrument agrees
with the theorem on the object's own assignment, trace by trace.

### 3. THE HONEST NEGATIVE (typed): the closing's su(2)-pair is trinification-native,
### not Pati–Salam-native
The naive relation Y/2 = T₃R + (B−L)/2, with T₃R = the closing's second su(2), does
**NOT** produce a physical B−L multiset: colored states appear at B−L = 5/3 and −4/3,
singlets at ±2 (physical would be ±1/3, ∓2/3, ±1, 0). Diagnosis: in the E₆
trinification frame the hypercharge mixes BOTH non-color Cartans beyond T₃R — the
closing's left-right pair is the trinification bidoublet structure (visible in the
lepton nonet), not the Pati–Salam decomposition. **B−L, if the closing carries it, is
a different Cartan combination.** Typed next cell (SP-1): solve for the B−L direction
as the Cartan element commuting with color and su(2)_L whose multiset is physical
(noting the L-vs-H_d identification ambiguity in the nonet that the solve must
resolve), and express it in the closing's coordinates — a small exact linear solve.

### Fences
Same as memo 23: symmetry-point structure only; no measured value enters or is
claimed; the state identifications in §1 are by quantum numbers (the standard names
attached to exact rows, CITED-grade dictionary); the §3 negative is against the naive
relation as stated, not against B−L existing. Gate 5 untouched.

### Certificates
`certificates/sm_table.py`; output `outputs/sm_table_out.txt`.

### One sentence for the ledger
Asked for the whole table, the closing wrote out one Standard-Model generation with
its exotics in exact quantum numbers and cancelled all eight anomaly traces to zero —
while honestly refusing the Pati–Salam reading of its second su(2), which is
trinification's bidoublet, leaving B−L as a solvable, typed next cell.
