# THE ASYMPTOTIC VALUE CHANNEL — the archimedean door is open, and the action is a growth rate
## (outside bench, 2026-08-21; fifth memo; computation `asymptotic_channel_out.txt`)

**The scope gap nobody priced.** Every banked value/scale no-go — the scale-torsor theorem
(Hom(G,ℝ₊)=0), the type law (B1032), frame-relativity (B936), k-blindness (B1012), the
seven sealed misses — quantifies over SINGLE-LEVEL invariants: one object, one equivariant
map, one frame. There is a class of canonical numbers those theorems never touch: **growth
rates of towers.** A rate is a difference of levels — it needs no basepoint, no frame, no
section: the torsor problem cannot even be posed for it. (The Jordan memo's line "arithmetic
sees torsor points without choosing" upgrades: asymptotics see VALUES without choosing.)

**The verified instance (this bench, three layers).** The object's own Kashaev tower
J_N(4₁) = Σₖ |(q;q)ₖ|², q = e^{2πi/N} — at every finite level exactly the kind of
finite/arithmetic object B1108 proved cannot carry the volume — carries in its growth:

    exponent  : 2π·(growth rate) = 2.0298832128847  vs banked Vol = 2.0298832128193
                (Richardson-refined; agreement 6.5×10⁻¹¹)
    power     : N^p with p = 1.500003  (= 3/2, the one-loop layer)
    constant  : 0.7598188  vs 3^{−1/4} = 0.7598357  (the torsion layer; disc(ℚ(√−3)) = −3)

The asymptotic itself is literature-standard (the volume conjecture is PROVEN for 4₁, and
4₁ is Zagier's original quantum modular form — Garoufalidis–Zagier; CITED, not claimed).
**The new content is where it sits in this programme's architecture:**

1. **B1108's negative is exactly right at every finite level and exactly wrong in the
   limit.** Arithmetic CS lacks Vol level-by-level; the TOWER of finite levels carries Vol
   as its growth rate. The archimedean place is the N→∞ boundary of the finite places —
   adelically standard, and it converts B1108's "two candidate doors" (Arakelov; quantum
   modularity) into one open door with a proven core on this very knot.
2. **The corpus is already computing this expansion without naming the principle.** Tree
   level = Vol (here, from the arithmetic tower); the 3/2 power and the 3^{−1/4} constant =
   the torsion/one-loop layer — which B1107 + the audit seat just built from the GEODESIC
   side (the Ruelle tower, 15 significant figures, two-bench). Two independent routes —
   quantum-arithmetic tower and length spectrum — into one expansion, with the quantum
   modular cocycle as the organizing object of all orders. (This also gives VI.3's residues
   a third instrument.)
3. **The withholding unifier, realized.** B518's factorization "SM value = (class
   invariant) × (substrate scale)" is exhibited by the tower: transcendental/archimedean
   exponent (the growth rate) × arithmetic coefficient tower (Ohtsuki coefficients, living
   in trace-field-flavored rings). The two factors the programme proved it holds separately
   are the two layers of one asymptotic expansion.
4. **THE ACTION IS THE GROWTH RATE OF THE DESCRIPTION.** S = −Vol·σ (B1088, zero free
   constants), and Vol is — verified above — the exponential growth rate of the object's
   own quantum state-sum. The minimal-description principle closes its own loop: what the
   object WEIGHS (its action) is HOW FAST ITS DESCRIPTION GROWS (an entropy). Where physics
   reads tower-growth numbers: entropies, central charges, anomaly and β coefficients —
   the dimensionless numbers physics actually derives are tower values, not level values.
   The seven crossings targeted level values — the channel the theorems close. The tower
   channel is the one the object provably keeps open.

**Firewall.** Vol is not an SM number; nothing here touches a measurement; Gate 5 intact.
The claim is scope-shaped: "the object refuses values" amends to **"the object refuses
LEVEL values and emits TOWER values."** Any future Tier-2 use of the channel must pass the
kind gate and CROSSING_REQUIREMENTS R1 — which tower invariants genuinely clear (B687's
atlas and B743's tower swept single-level data only; growth-rate invariants are outside
both by construction).

**Proposed cells.** C-V1: bank the three-layer extraction (lock = the script; fast).
C-V2: the Ohtsuki coefficient arithmetic — fit c₁, c₂ and test membership in the
trace-field ring (CITED tables exist); the coefficient tower as the arithmetic factor.
C-V3: the cocycle assembly — quantum modularity joining B1090's Andersen–Kashaev saddle
(= ω) and B1107's Ruelle tower as three faces of one state-integral. C-V4: enumerate the
channel's dimensionless invariants (coefficient ratios; modular-defect data) as the first
crossing-candidate CLASS outside every searched space — nomination-gated per B1026, no
candidate asserted here.
