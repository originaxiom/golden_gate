# LORENTZ ON THE DOUBLE — where the 3+1 signature actually lives, and what it costs
## (outside bench, 2026-08-21; third memo; computation `lorentz_double3_out.txt`; the failed
## first method `lorentz_double2_out.txt` kept for the error ledger — its printed "VERIFIED"
## lines are WRONG, superseded by the canonical construction.)

**The one-line lemma that reorganizes the question.** A real form g₀ of a complex Lie algebra
can never contain a complex subalgebra (s = is and g₀ ∩ ig₀ = 0 force s = 0). The hatch's
composed holonomy is Zariski-dense in one complex SL(2,ℂ) — so **it cannot sit inside any
real form of E₆**. The programme's banked "the native gauge system is inherently COMPLEX
(complex CS of E₆(ℂ), B715)" is thereby EXPLAINED, not just observed: complexity is forced
by hosting the object's holonomy at all.

**The realification fact (standard, cited).** so(3,1) ≅ sl(2,ℂ)_ℝ, and its complexification
is sl₂ ⊕ sl₂ with complex conjugation SWAPPING the summands (the self-dual/anti-self-dual
halves). To host Lorentz, a real form of E₆ needs TWO commuting sl₂s of the SAME class,
exchanged by an antilinear involution. One copy of the object supplies one sl₂. **Two copies
— the mirror double — supply the pair, and the mirror IS the conjugation (VII.1, verified at
the cusp: the amphichiral gluing = the ℚ(√−3) Galois twist).**

**The verified algebra (this bench, exact, canonical construction).** At the A2 landing:
the roots orthogonal to the A₂ form an A₂+A₂ subsystem (12 roots, components 6+6); the
centralizer z = I₁ ⊕ I₂ = sl₃ ⊕ sl₃, both ideals **spans of root vectors + coroots — ℚ-rational
by construction** (the first attempt via generic-element primary decomposition FAILED — 8+9,
mixed factors — and is superseded; error logged). Then:
- the principal JM triple of I₁ constructed and verified ([e₂,f₂]=h₂ etc., exact);
- it COMMUTES with the hatch's A2 triple;
- it is **the same class**: dim z_{e₆}(e₂) = 36 = A2 — so the pair is swappable, as a
  conjugation-glued Lorentz pair must be;
- **the joint centralizer of the two commuting A2 triples is exactly 8-dimensional — one
  su(3).** Realify the pair: **E₆ ⊇ so(3,1) ⊕ su(3): Lorentz plus color, and nothing else.**

**THE SIGNATURE THEOREM-SHAPE (the point).** The two sl₃ factors are ℚ-rational, so the
object's own arithmetic conjugation (q ↦ 1−q — which IS the mirror) fixes each factor and
does NOT provide the antilinear swap. The swap — the choice of real structure, i.e. **the
signature — is not in the object. It is the observer's gluing of the two mirror copies.**
B716 ("signature is the observer's") and B155/B517 ("(1,3) is algebraic inertia") are hereby
exhibited at the Lie-algebra level, constructively: the object hands over sl₂ ⊕ sl₂ ⊕ su(3)
over ℚ; conjugating the two hands into one Lorentz algebra is the act of observation. The
capstone's sentence "spacetime needs both hands" is now a computation.

**THE DOUBLE-DUTY COMPETITION (new, sharp, priced).** The same ideal I₁ hosts BOTH
candidates for its own spending:
- its **principal sl₂** (A2-class, verified here) — the Lorentz partner;
- its **block su(2)×u(1)** (B1098's exhibited electroweak seat).
One su(3) factor; spend it on GRAVITY (realification partner → so(3,1)⊕su(3)_color survives)
or on ELECTROWEAK (B1098's gauge chain → su(3)_c × su(2) × u(1) at rank 4, no Lorentz).
**Gravity and electroweak compete for the same trinification factor** — a new instance of the
B963 one-resource pattern (τ's double duty), one level up. This is a structural PREDICTION of
tension exactly where the programme has always found the going hardest (EWSB, the 4d lift),
and it names the reconciliation problem precisely: a real form and an embedding serving both
readings at once, or a proof none exists. Either outcome banks.

**Named residual (the one uncomputed step).** Which real form(s) of E₆ contain
so(3,1) ⊕ su(3) ⊕ (the u(1)-traces) with su(3) compact — a finite check against the
Berger/Karpelevich subalgebra tables; the natural candidate is E₆(−26) = M(𝕆,ℂ), the
corpus's OWN identified build (B882/B904), whose maximal compact F₄ ⊃ su(3)×su(3)/... —
CITED-flagged, not claimed. If E₆(−26) hosts it, the programme's Jordan-algebra presentation
was the gravitational real form all along.

**Proposed cells.** C-L1: the real-form check (table lookup + one involution construction;
small). C-L2: the double-duty adjudication (does any single real form support both readings
simultaneously? two-outcome). C-L3: re-read B1104's 4d-suspension NO-SECTION beside this —
the suspension asked the OBJECT to name a 4th dimension and proved it cannot; this memo says
the 4th dimension was never the object's to name: it is the realification datum, observer-
supplied — the two results are the negative and positive faces of one statement and should
be cross-cited.
