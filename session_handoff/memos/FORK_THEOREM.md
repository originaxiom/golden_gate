# THE PRICE OF SPACETIME — the D5 fork is a centralizer theorem, the three A2's are a frame torsor, and the corpus is running two color frames without knowing it
## (outside bench, 2026-08-22; eleventh memo; every numbered claim verified exactly over ℚ)

### 1. THE LADDER (verified: `certificates/fork_theorem.py`, exact ranks)
On the banked A2 landing (S₀ = the Levi-(0,2) A2 carrying triple₁; S₁, S₂ the orthogonal
A2 pair; triple₂ = principal of sl(S₁); color = sl(S₂)):

> dim z(T₁) = **16** · dim z(T₁ ∪ color) = **8** · dim z(T₁,T₂) = **8** (B1114's number)
> · dim z(T₁,T₂ ∪ color) = **0**.

The last zero is the theorem: **once the double is glued into so(3,1) and color is
reserved, there is NO direction anywhere in e₆ commuting with both — no hypercharge, no
u(1), nothing.** If the glued so(3,1) is spacetime Lorentz, a gauge hypercharge must
commute with it (spacetime × internal factorization), and e₆ has none to sell. On the
other branch, z(T₁ ∪ color) = 8 = sl(S₁) exactly — an entire sl₃ of electroweak room
with a rank-2 Cartan, which is precisely where B1102's 18 hypercharge directions live.
**So B1115's D5 fork (EW ⊻ internal-Lorentz, exclusive) is upgraded from a
spending-order observation to a centralizer theorem: this E₆ can pay for any TWO of
{spacetime Lorentz, compact color, hypercharge/EW} — never all three.** The tenth memo's
simultaneous closing is therefore also MAXIMAL: so(3,1) + su(3)c is the largest
SM-facing purchase the algebra supports in one conjugation; the EW sector must ride the
other fork branch or live outside this e₆. (That gravity-vs-gauge exclusivity is
B1115's reading; the exact dims are new.)

### 2. THE FRAME TORSOR (verified, same certificate)
The slot-preserving subgroup of W(E₆) maps ONTO the full S₃ of the three A2's with
perfectly uniform fibers: **each of the 6 permutations of {S₀,S₁,S₂} is realized by
exactly 216 = 6³ = |W(A₂)|³ Weyl elements** — i.e. N_W(A₂³)/W(A₂)³ ≅ S₃, full and
clean. The object does not distinguish the three A2's: **"which factor is color" is a
frame choice**, exactly as "which Fibonacci rule" was at the origin torsor. The
programme is bracketed by torsors — a Klein torsor of rules at the entry, an S₃ torsor
of frames at the exit into physics — and the observer picks base points at both ends.
(Last sentence framing, labeled; the torsor itself is computed.)

**A ledger flag for the banking seat (correction-grade, no contradiction):** the corpus
is running TWO color frames. B1102/B1118 build hypercharge on the Cartan of the two A2's
orthogonal to the landing — implicitly color = S₀ (the landing A2), EW pair = {S₁,S₂}.
B1114/B1125/B1127 place color = S₂ (the joint centralizer), Lorentz pair = {S₀,S₁}.
These are S₃-frame-related readings of one structure, consistent BECAUSE of the fork
(each branch picks its own frame and the branches are exclusive) — but the ledger should
record the frame per arc, or the two color labels will eventually be composed as if they
were one.

### 3. P IS A TRANSPOSITION, AND THE CLOSING IS CP (structural + framing, labeled)
B1118's hypercharge-fusing "mirror swap" (3,2,1,0) exchanges the p₁-block and p₂-block
of the hypercharge Cartan — it is the transposition S₁↔S₂. The simultaneous closing's
swap is the transposition S₀↔S₁. Same S₃ conjugacy class: **every parity-like act the
observer performs — fusing the two hypercharge orbits, gluing the double into so(3,1) —
is one S₃ transposition, dressed for its branch.** And the closing factorizes as
σ = τ ∘ θ = (conjugation at the ∞-place) ∘ (an arithmetic transposition-lift) = **C∘P.
The observer's single act is a CP operation.** The ledger {C, P, spin lift} then reads:
C = τ (the archimedean conjugation), P = the S₃ transposition (available in the object's
own arithmetic W, chosen by the observer), and the spin lift — the one bit B1122 proved
PAIR-INVISIBLE (a central sign no coupling can read) — is exactly the one bit no CP act
pays. One closing pays C and P together; spin stays forever a separate purchase.

### Open cells (typed, not claimed)
- **F-1 the EW real form**: on the EW branch the room is exactly sl(S₁); which real form
  does a fork-compatible conjugation induce on it — su(3)? su(2,1)? (su(2)×u(1) must
  survive inside it); the analog of the tenth memo's sweep, one slot over.
- **F-2 the ambient escape**: is "two of three" E₆-specific? The same ladder computed in
  e₇/e₈ (does a bigger algebra sell all three at once, and at what price?).
- **F-3 the frame audit**: re-read the banked arcs tagging each with its S₃ frame.

### Certificates
`certificates/fork_theorem.py`; output `outputs/fork_theorem_out.txt`.

### One sentence for the ledger
The algebra sells any two of spacetime, color, and hypercharge — never three — the three
A2's it sells them from are a single S₃ torsor of frames, and the observer's purchase is
always a CP act: conjugation at infinity times a transposition of the torsor, with the
spin bit alone left forever unpaid.
