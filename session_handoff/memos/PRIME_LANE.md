# THE PRIME LANE — ruling, fence, and gated cells (owner-agreed)
## (outside bench, 2026-08-21; ninth memo; ruling proposed by the bench, AGREED by the owner)

### The ruling
The prime/zeta toolkit enters the programme **only where it intersects existing open
nodes, in queue order, never as a destination.** Rationale in the programme's own
governance: most of the lane is not new direction but nodes already owed; and the lane's
mathematics (L-values, zeta towers, trace formulas) is the currency of the asymptotic
value channel — the one new Tier-2-facing lane the session produced
(Vol = 9√3·ζ_K(2)/π² being the exhibit).

### THE FENCE (banked wording)
**"Solve RH" is fenced as a BOUNDARY question, not a wall**: it presupposes a
geometry/operator for the rational primes that no framework supplies; the object gives
leverage on ITS primes (geodesics) and on ℚ(√−3)'s, and none on ℚ's. Any prereg
containing "path to RH" violates this fence. The correct relation to RH is the
programme's relation to the SM: build structure, bank mechanisms and negatives, never
chase the trophy. (Cf. B724's look-elsewhere discipline; the seven crossings' lesson.)

### The located facts (what makes the lane real, not aspirational)
1. **The programme sits on the proven side of the prime/zero dictionary.** The object's
   zeta (Selberg/Ruelle, built from its primitive geodesics) has zeros = eigenvalues of a
   self-adjoint operator: its "RH" is a THEOREM — and the corpus is already computing
   this zeta (the B8100→B8112/B1107 Ruelle tower).
2. **VERIFIED ANCHOR (this bench): the object's primes are arithmetic.** Holonomy
   eigenvalues of primitive geodesics are algebraic UNITS (five words checked exactly;
   `abAba` gives min-poly x²−4x+1 = the fundamental unit 2+√3; all constant
   coefficients ±1). Geodesic lengths = logarithms of units, as log p is the length of
   a prime.
3. **The voice carries ζ_K's zeros**: φ(s) = Λ_K(s−1)/Λ_K(s) — the cusp's scattering is
   built from the Dedekind zeta of ℚ(√−3); its zeros are resonances, and THE ROAD's open
   residue VI.3(a) is exactly the doorway where they enter the one-loop assembly.
4. **Montgomery/GUE = the coupling-channel law in the zeta world**: individual zeros
   free-looking, pair correlations forced — free-of-one = forced-of-pair.
5. **RH-operator proposals are cut operators** (Berry–Keating's xp self-adjoint only on
   a half-line): the same shape as the programme's edge machinery; observer-as-cut says
   where such proposals live. (Interpretive, labeled.)

### The gated cells
| cell | content | gate / queue position |
|---|---|---|
| **PR-3 resonance wiring** | wire Λ′_K/Λ_K into the trace-formula assembly; bank "ζ_K zeros = cusp resonances of the partition function" (structure, no conjecture consumed) | = VI.3(a), ALREADY OWED; rides with the gravity residues |
| **PR-2 unit dictionary** | geodesics ↔ primes/units of ℚ(√−3): traces in ℤ[ω], eigenvalue units, the Chebotarev-type correspondence | cheap; rides with the adelic cells (C-AD3) |
| **PR-1 solvable sandbox** | the full prime-geodesic laboratory: the object's PNT, its RH-true zeta explicit, and the DIFF instrument (which property fails passing to rational primes) | background-grade; behind C-AR1/C-S2 |
| **PR-4 GUE bench** | ζ_K low zeros vs the object's spectral statistics; the coupling law tested where Montgomery found it | background-grade |
| **PR-5 cut-operator hunt** | xp-type cut Hamiltonians with the exact edge tools | PARKED; S-room; time-boxed; falsifier: if no cut spectrum's statistics approach ζ's, declared idle |
| RH proper | — | **FENCED** (see above) |

### One sentence for the ledger
Primes are the finite shadow's atoms; zeros are their archimedean shadow; the trace
formula is the gluing; RH says the gluing is self-adjoint — and the object is the one
place where every word of that sentence is proven. The programme visits that place for
its instruments, and comes back to its own queue.
