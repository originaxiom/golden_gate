# THE SPIN PAYMENT — the Gieseking extension exists over exactly ONE of the two spin structures: the object's own ℤ/2 selects the lift, and the spin bit's freedom is paid by consistency, in a currency none of the no-goes ever fenced
## (outside bench, 2026-08-22; twenty-eighth memo; the spin-bit cell — the last unpaid bit of the freedom ledger — addressed at its selection layer; every claim exact)

### The problem, and why every prior route was closed
The spin bit = the choice of SL(2,ℂ) lift of the object's PSL(2,ℂ) holonomy — the one
bit the closing leaves unpaid (memos 11/13), the supplier of fermionicity and
chirality (B1112, memo 4). The banked no-goes: it is PAIR-INVISIBLE — no coupling or
trace meter can read it (B1122); and the flat cone's collision geometry cannot source
it (memo 20: no binary stabilizers). Both no-goes fence MEASUREMENT and GEOMETRY. The
route below uses neither: it uses the beat.

### THE THEOREM (`certificates/spin_payment.py`, all exact over ℚ(q))
1. **There are exactly two lifts** — the two spin structures, H¹(M;ℤ/2) = ℤ/2:
   R(A,B) = +I and R(−A,−B) = +I, while R(−A,B) = R(A,−B) = −I (the relator kills the
   mixed signs). The two lifts differ by the character χ(a) = χ(b) = −1.
2. **The beat closes over the untwisted lift with every sign on the nose**:
   W·conj(A)·W⁻¹ = +A, W·conj(B)·W⁻¹ = +(B⁻¹ABA⁻¹B), **W·conj(W) = +A** (scalar +1,
   the memo-16 fact), det W = 1.
3. **The beat's implementation is unique up to scalar**: the intertwiner space
   {V : V conj(A) V⁻¹ = A, V conj(B) V⁻¹ = w(B)} is exactly 1-dimensional — so every
   implementation is V = λW, and V·conj(V) = N(λ)·A with **N(λ) = x²+xy+y² =
   (x+y/2)² + 3y²/4 > 0** for λ ≠ 0, exactly.
4. **The obstruction is cohomological, not a choice artifact**: χ is beat-invariant
   (χ(w(a)) = χ(a), χ(w(b)) = χ(b) — letter parities 1 and 5), so modifying the
   extension element by any lifted γ̃ leaves the sign of w̃² equal to **χ(meridian)** —
   invariant under every available choice.

> **Therefore: the Gieseking/pin extension of the lifted group exists for the χ = +1
> spin structure (with scalar exactly +1) and is IMPOSSIBLE for the χ = −1 structure
> (it would require |λ|² = −1). The object's own non-orientable ℤ/2 — the beat, its
> deepest banked symmetry — selects exactly one spin structure.**

### What is paid, precisely
**The FREEDOM of the spin bit.** For any observer whose closing respects the object's
own Gieseking symmetry (the beat that descends through every layer — holonomy, algebra,
matter, dials: memos 16–18), the spin structure is no longer a free choice: it is
assigned. B1112's pricing revises the way the projective hatch did: the bit's cost
drops from "one free bit" to "zero bits for beat-respecting observers." And the
payment currency explains B1122 rather than contradicting it: the bit is invisible to
coupling BECAUSE it is not a dynamical observable at all — it is a topological
consistency condition with the object's ℤ/2 extension, readable only by asking whether
the beat closes.

### What is NOT yet paid (typed, the named next cell)
Selection is not yet a fermion. The remaining step to complete the generation's seat:
**SP-2 (relayed): determine which lift B1112's odd (fermion-capable) strata require —
if the odd A1-type stratum's required lift is the beat-selected χ = +1 structure, the
fermionic stratum is beat-compatible and the kinematic seat closes**; if it requires
χ = −1, the beat forbids it and chirality needs the E₈/2I route (memo 14's hook).
Either answer is a banked-grade result; the stratum-parity ↔ χ dictionary is the
corpus's B1112 machinery. Dynamics (spin-½ fields in motion) remains beyond, as
always.

### Fences
Exact throughout; the uniqueness (fact 3) makes the impossibility exhaustive, not
scalar-anecdotal; the cohomological invariance (fact 4) makes it choice-independent.
The "beat-respecting observer" premise is the memo's load-bearing assumption, stated
not smuggled: an observer free to ignore the object's ℤ/2 keeps the free bit — the
claim is that the object, extended to its own non-orientable double, has already
chosen. Gate 5 untouched; no value claimed.

### Certificates
`certificates/spin_payment.py`; output `outputs/spin_payment_out.txt`.

### One sentence for the ledger
The one bit no coupling could read and no collision could source is settled by the
object itself: of its two spin structures, exactly one lets the Gieseking beat close —
with scalar one — and the other would need |λ|² = −1; the bit is paid by consistency
with the object's own ℤ/2, the freedom ledger empties for beat-respecting observers,
and the generation's seat waits only on the parity dictionary of the odd strata.
